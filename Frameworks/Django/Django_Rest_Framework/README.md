# Django Rest Framework

## Setup

1. Install
	- `<env_manager> install djangorestframework

2. Add `DRF` to `~/settings.py > `INSTALLED_APPS`
	```python
		...other code
			rest_framework
		...other code
	```